<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="RoadSense Collector"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.RoadSenseCollector">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CollectorActivity" />
        <activity android:name=".LibraryActivity" />
        <activity android:name=".StatsActivity" />
        <activity android:name=".ExportActivity" />
        <activity android:name=".PhotoViewerActivity" />
        <activity android:name=".GalleryBrowserActivity" />
        <activity
            android:name=".VideoExtractorActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize" />

        <!--
            CropActivity: fullscreen dihandle via WindowManager.FLAG_FULLSCREEN di kode
            (sudah diberi @Suppress("DEPRECATION")).
            Theme NoActionBar dipakai agar tidak ada title bar tanpa bergantung
            pada android:style/Theme.Black.NoTitleBar.Fullscreen yang deprecated.
        -->
        <activity
            android:name=".CropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.RoadSenseCollector.Fullscreen" />

    </application>

</manifest>